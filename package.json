{
  "name": "nft-marketplace",
  "version": "1.0.0",
  "description": "NFT Marketplace Project",
  "scripts": {
    "install-all": "npm install --prefix blockchain && npm install --prefix server && npm install --prefix frontend",
    "deploy": "npm run deploy --prefix blockchain",
    "samples": "npm run samples --prefix blockchain",
    "serve": "npm run serve --prefix server"
  },
  "author": "",
  "license": "ISC"
}
